name: Finalize corporate hardening

on:
  push:
    branches:
      - hardening/corporate-safe
    paths:
      - .github/workflows/finalize-corporate-hardening.yml
      - tools/finalize_corporate_hardening.py
  workflow_dispatch:

permissions:
  contents: write

env:
  SOLUTION: Win32CaptureSample.sln
  CONFIGURATION: Release
  PLATFORM: x64

jobs:
  finalize-and-build:
    runs-on: windows-2022
    steps:
      - name: Checkout hardened branch
        uses: actions/checkout@v4
        with:
          ref: hardening/corporate-safe
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Apply final hardening cleanup
        shell: pwsh
        run: python tools/finalize_corporate_hardening.py

      - name: Verify corporate-safe source contract
        shell: pwsh
        run: python tools/verify_corporate_hardening.py

      - name: Check patch whitespace
        shell: pwsh
        run: git diff --check

      - name: Commit hardened source back to branch
        shell: pwsh
        run: |
          if (-not (git diff --quiet)) {
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add Win32CaptureSample/Canvas.cpp tools/verify_corporate_hardening.py
            git commit -m "hardening: remove registry launcher fallback and external URLs"
            git push origin HEAD:hardening/corporate-safe
          } else {
            Write-Host "No source changes required."
          }

      - name: Set up NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        shell: pwsh
        run: nuget restore $env:SOLUTION -NonInteractive

      - name: Set up MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build Release x64
        shell: pwsh
        run: msbuild $env:SOLUTION /m /p:Configuration=$env:CONFIGURATION /p:Platform=$env:PLATFORM /v:minimal

      - name: Verify executable and calculate SHA256
        shell: pwsh
        run: |
          $exe = Join-Path $env:GITHUB_WORKSPACE "x64\Release\SpatialCanvas.exe"
          if (-not (Test-Path $exe)) { throw "SpatialCanvas.exe was not produced" }
          Get-Item $exe | Format-List FullName,Length
          Get-FileHash $exe -Algorithm SHA256

      - name: Upload finalized hardened executable
        uses: actions/upload-artifact@v4
        with:
          name: SpatialCanvas-corporate-safe-x64-final
          path: x64/Release/SpatialCanvas.exe
          if-no-files-found: error
          retention-days: 7
